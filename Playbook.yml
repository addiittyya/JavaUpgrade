---
- name: Java upgrade
  hosts: web
  remote_user: root
  vars_files:
  - /etc/ansible/variable/javavar.txt

  tasks:
  - name: Java Upgrade
    yum:
      name: "{{ package }}"
      state: "{{ action }}"